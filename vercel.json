{
  "version": 2,
  "builds": [
    {
      "src": "api/main.py",
      "use": "@vercel/python",
      "config": {
        "includeFiles": ["api/q-vercel-latency.json"]
      }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/main.py" }
  ]
}
